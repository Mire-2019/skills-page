# 技能适配（skill-adapt）变更记录

本 skill 自身版本采用 frontmatter `version` + 本文件记录。

## v1.4.0（2026-09-12）—— 面向公开发布的可移植性改造

**背景**：这套流程原本只在一台机器上跑，文件里散落着本机路径、私人 skill 名和内部术语。
公开发布后，外部读者拿到手「不知道怎么装、装了跑不动、一半引用看不懂」。本版做可移植性改造。

- **新增「本地约定」配置块**：原先散落在正文里的本机路径、验收命令、画像来源、近亲清单，
  收拢到 `SKILL.md` 开头一处，正文改用 `<staging_dir>` 这类占位符引用。
  **方法部分现在与机器无关**；作者自己用仍是零改动。
- **修复 `allowed-tools` 缺 `WebSearch`**（正文在用但未声明，属契约不一致，会实际报错）
- **版本号解耦**：正文两处误写 `version: 1.1.0`，与 frontmatter 冲突；
  改造产物的版本号改名为 `adaptVersion`，与本 skill 自身的 `version` 分开。
- **修复决策点清单模板**（原表格半截损坏，`│ 2. …` 悬在表格外）
- **清理内部变更语言与内部专有名词**（「本轮新增」「见 Q26」「Q24 定」等；
  内部企业名统一泛化为通用描述）
- **移除对私人 skill / 私有记忆系统的硬引用**，改为配置项 + 通用描述
- **新增「不适用场景」「定位」两节**，明确边界
- **新增**：`README.md`（安装步骤 / 可移植性说明 / 产物示例 / 目录结构）、`LICENSE`（MIT）、
  `.gitignore`、`skill-reports/`（真实审查报告，已脱敏）

## v1.2.0（2026-08-21）—— 三道工序产出升级 + 决策点前置 + 隐藏价值观识别
- **新增铁律 8「隐藏价值观一票识别（剥壳不否决）」**：技能里凡出现评分/分级/会话回流算分/趋势weakness追踪/督促复盘等字眼 → 判定作者把价值观考核体系包装进 skill；处置 = 剥壳重定本地标准（评价人→评价指令），不直接搬外部价值观；「带考核」≠「安全否决」。
  - 来源：prompt-boost 适配中发现原版 15 维评分是**某企业价值观考核量表包装**（含员工会话蒸馏回流算分），用户识别并要求重定标准。
- **价值分析（第 1 步）产出升级**：单薄矩阵 → **四段式分析**（工作原理 / 设计意图 / 适用边界 / 隐含假设与本地张力）+ 安全栏 + 隐藏价值观检查。价值分析一次输出，不做多轮追问；拿不准标待确认清单。
- **整合分析（第 2 步）扩展**：对候选 skill 的近亲逐个对话（关系 / 取舍 / 若合并），不只看撞名。
- **适配改造（第 3 步）流程反转**：**先出「改造决策点清单」逐项拍板，再写正文**；新文件 `references/decision-points.md` 提供模板（决策点 / 现状→建议 / 原理理由 / 不改会怎样 / 恢复代价 / 拍板）。
  - 反面教训：曾自作主张砍掉一个模式、只给结论没给理由 → 用户追问才发现误判 → 整套返工。决策点前置正是为堵这个。
- **local-adapt 六步 → 七步**：第 2 步插「[决策点清单]」，重写 / 融合时加铁律 8 剥壳动作，报告段含决策点结论。
- 文件地图新增 `references/decision-points.md`。

## v1.1.1（2026-08-20）—— 铁律 7 + local-adapt.md，钉死「外部一律改造后装」
- 新增**铁律 7『默认强制适配改造，绝不裸装』**：所有外部 skill 一律经三道工序改造产出本地改造版后才允许进体系，没有「直接原样装进来」路径；例外仅 ④只借鉴不装（重复 / 无价值 / 安全否决时存借鉴笔记）。
- 定位段补「skill 分两类」树图：自建 → creator，外部 → adapt（一律改造）。
- 新增 `local-adapt.md`：六步落地规程（领名建档 → 重写融合 → 脚本合规 → 三重验证 → 报告拍板 → 装后更新）+ 默认贴合项清单 + ④例外。
- SKILL.md 版本 1.0.0 → 1.1.0。

## v1.1.0（2026-08-20）—— 首次落地改造：skill-creator 本地化
- 用本 skill 的 ②改造 流程对 `anthropics/skills#skill-creator` 做了**本地化改造**（而非直接装原版）：
  - 头部加「铁律」5 条（先 grill 后创造 / 不与 adapt 冲突 / 产物落工作盘 / 正文与用户母语一致 / 描述带触发劲）；
  - 加「本地画像取材」章（读项目约定与记忆索引）；
  - 本地化标注：`source:` + `adaptVersion` + 文末指向本台账。
- 效果：以后新建 / 优化 skill 时，自动先走需求打磨、并把外部 skill 一律转介给本 skill —— 实现了「一次调用即隐式完成 grill + 本地化适配」。

## 说明

- 更早的实验性改动未单独留条。
- 线上若与本文件不一致，以仓库为准；本机与仓库两份曾并行修改，同步时请人工对一遍 `references/`。
