---
name: skill-adapt
description: "把「要装的新 skill」当原料，经三道工序（价值分析→整合分析→适配改造）适配到本地后给用户拍板，装不装由用户定。当用户拿来一个新 skill（B站链接/公众号文/GitHub 仓库/或说『帮我装 xxx skill』），或想评估某个外部 skill 是否值得装、怎么改造得贴合本地时使用。"
allowed-tools: [Bash, Read, Write, Glob, Grep, WebFetch, WebSearch, Skill]
version: 1.4.0
license: MIT
---

# 技能适配（skill-adapt）

把外部 skill 当作「原料」，经三道工序（**价值分析 → 整合分析 → 适配改造**）处理后，产出「已适配到本地、可拍板可装」的成品。

**本 skill 只做「怎么装」的决策辅助，不替用户拍板；装不装永远由用户定。**

---

## ⚙️ 先改这里：本地约定

**下面 8 行是全篇唯一需要改的地方。** 正文从此只引用 `<staging_dir>` 这类占位符，不再出现具体路径。

默认值是一台 Windows 工作机的配置，**换成你自己的即可**。用不到的项留空，对应章节会自动降级。

```yaml
staging_dir:   D:\ClaudeCode\skills-staging      # 暂存区：下载不安装，只读
install_dir:   D:\ClaudeCode\skills              # 改造版最终落到哪
agent_dir:     ~\.claude\skills                  # agent 读取 skill 的目录
report_dir:    ./skill-reports                   # 审查报告 / 借鉴笔记
verify_cmd:    npx skills list                   # 装完拿什么确认（Windows 需在 PowerShell 里跑）
memory_index:  MEMORY.md                         # 你的项目约定 / 记忆索引（没有就留空）
office_suite:  lark-cli                          # 你用的办公套件内置能力（没有就留空）
kin_skills:    grilling, creator, office-hours   # 你本地已有的同类 skill（基线扫描得到）
```

> **为什么这么设计**：这套方法本身是通用的，但它落地的样子高度依赖你的机器。
> 把机器相关的部分收拢成一处，方法就不会被环境细节淹没 —— 别人拿去能用，你自己改也不用来回翻。

---

## 定位：skill 分两类

```
自建 skill（从 0 创作） ── 走创作类 skill（先磨需求 → 再打磨评测）
外部 skill（要装进来）  ── 走 skill-adapt（三道工序 → 本地化改造 → 用户拍板）
                              └─ 铁律：外部 skill 一律走适配改造，绝不直接装
```

---

## 铁律（最高优先级）

1. **永远被动触发**：不主动跑。用户给了新 skill 来源才开工；装完确认后即止，不做订阅 / 定期扫描。
2. **下载不安装**：外部 skill 一律 `git clone --depth 1` 到 `<staging_dir>`，**绝不**直接装进来。
3. **三层基线扫描**：判断「本地已有的」从 3 层看 —— agent 技能层 / 生态登记层 / 办公套件内置能力层。外部协作类 skill 与内置层撞 = **能力重复**（有更好替代），**不**等同「已安装」。
4. **安全栏一票否决**：价值分析的固定栏目。发现 SKILL.md / scripts 有可疑外传（curl 把数据发往未知域名）、索取敏感凭据、操作禁止路径 → **标红不装**，并给理由。**这一条没有例外。**
5. **工作盘原则**：暂存区、报告库、改造产物一律落**工作盘**，不落系统盘。默认值见配置块，按你的机器改。
6. **只决定怎么装，不产生新安装**：三道工序各自结束处都有决策门，等用户拍板；用户把关后，由 agent 直接装完这条链路，判定标准 = 跑 `<verify_cmd>` 能看到该技能。
7. **默认强制适配改造，绝不裸装**：**所有**外部 skill 一律先经三道工序改造（①组装 / ②改造 / ③定制），产出**本地改造版**后才允许进入体系。
   例外仅两档 —— 判「重复 / 无价值」或「安全一票否决」时走 ④只借鉴不装（存借鉴笔记，不搬正文）。
   **没有「直接原样装进来」这条路径。**
8. **隐藏价值观一票识别（剥壳，不否决）**：价值分析的固定栏目。外部 skill 里凡出现 **评分 / 分数、分级 / 成长等级、会话数据回流算分、趋势 / weakness 追踪、督促 / 复盘督促** 这类字眼 → 判定作者把**自己的价值观考核体系**包装进了 skill。
   处置：**不直接搬运**，剥壳重定本地标准（如「评价人」→「评价指令」）；涉及删除 / 替换的决策进决策点清单由用户拍板。
   **「带考核性质」不等于「安全否决」** —— 不因价值观异同拒绝安装。
   > 实例：某外部 skill 的「15 维评分表」实为一套企业价值观考核量表，适配时被识别并剥除，只保留其有用的指令结构。

---

## 三道工序

### 第 1 步 · 价值分析（检 + 过滤）

**触发**：收到新 skill 来源（链接 / 文章 / 指令）。

1. **解析来源**：按 [source-routing.md](references/source-routing.md) 的映射把输入转成 skill 名单 ——
   - 视频 / 播客链接 → 用你本地的视频拆解类 skill 提取内容；
   - 公众号 / 文章 / 网页 → WebFetch 抓正文；
   - GitHub 仓库 → 拉 README + 找 SKILL.md；
   - 纯文字 /「帮我装 xxx」→ 当关键词，走生态检索命令 + WebSearch 补齐。
2. **下载不安装**：`git clone --depth 1` 到 `<staging_dir>\<owner__repo>\`，**只读** SKILL.md + references。
3. **三层基线扫描排除**（见铁律 3）：
   - agent 层：`<agent_dir>` 下已装的；
   - 生态登记层：`<verify_cmd>` 列出来的；
   - 能力层：`<office_suite>` 这类内置能力 → 当「能力替代品」比较。
4. **全网检索**：WebSearch + skill 生态站点，罗列有无其它同类，判断互补 / 重复 / 独立。
5. **两个固定栏目**：
   - **安全栏**（铁律 4）；
   - **隐藏价值观检查**（铁律 8）。
6. **输出四段式分析**：

   ```
   候选 skill × 三层基线（重复 / 互补 / 无交叉 + 理由）

   ① 工作原理：它怎么转起来（触发机制 / 各步骤动作 / 依赖什么输入输出）
   ② 设计意图：作者想解决什么问题（站在作者视角推断，不臆断）
   ③ 适用边界：什么场景有效、什么场景失效
   ④ 隐含假设与本地张力：它对环境的预设（工具链、路径约定、权限与凭据范围、
      数据去向）与本地原则（安全性 / 数据不出域 / 工作区规范）有无冲突点

   结论：值得装 / 不装（重复 / 无价值 / 安全否决）→ 理由
   ```

   价值分析**一次输出，不做多轮追问**；拿不准的判断标注（事实 / 推断 / 待确认），写进待确认清单让用户定夺。
7. **等用户决策** → 有价值 → 进第 2 步；无价值 → 借鉴笔记留档（存进 `<report_dir>`），流程结束。

### 第 2 步 · 整合分析（比 + 合）

对候选 skill vs 本地相似 skill：

1. 出**优劣对比表**。
2. **近亲逐个对话** —— 不只看撞名，凡「领域相近 / 职责相近」的都逐个过：

   ```
   候选 vs 近亲（<kin_skills>）

   a. 关系：互补 / 重复 / 张力（为什么）
   b. 取舍建议：哪个留、哪个并、怎么分工
   c. 若合并：合并后的职责边界，一句话说清
   ```
3. **拆散 → 重组 → 合并且标注**：本地的都用本地、外部更强的用外部、重复的剔除。
4. 输出**整合方案** → **等用户确认**。

### 第 3 步 · 适配改造（改 + 造）

> **流程反转（本方法的核心）：先出决策点清单逐项拍板，再写正文。**
> 把改动决策前置，杜绝自作主张。

1. **读本地约定**：按 [profile-reading.md](references/profile-reading.md) 读 `<memory_index>` 及其指向的相关文档。
2. **定改造类型**（四档，默认 ①，每次由用户拍板）：
   - **① 组装**（默认）：外部系列 skill 拆散重组成本地一套模块；
   - **② 改造**：拿到外部 SKILL.md 改写其运行逻辑（加目录规范、改命令、加强约束）；
   - **③ 定制**：外部只当参考模板，本地主力重建；
   - **④ 只借鉴不装**：无价值 / 重复 / 有风险 → 存一条「借鉴笔记」（一行要点 + 来源链接）到 `<report_dir>`，不搬正文。
3. **产出「改造决策点清单」**（置于任何改动之前），让用户逐项拍板：

   | # | 决策点 | 现状 → 建议 | 原理 / 理由 | 不改会怎样 | 恢复原版代价 | 拍板 |
   |---|---|---|---|---|---|---|
   | 1 | | | | | | 等拍 |
   | 2 | | | | | | 等拍 |

   表格模板见 [decision-points.md](references/decision-points.md)。
   **每一项都要写原理和理由**，不是只写「砍掉 XX」。所有改动先经用户拍板，才能进下一步写正文。

   > **为什么这么较真**：有过一次教训 —— 自作主张砍掉一个模式，只给了结论没给理由；
   > 用户追问才发现砍错了，整套返工。决策点清单就是用来防这类返工的。
4. **写正文**（拍板通过后才动 SKILL.md）：改造落点 `<install_dir>`，保留 `source: <owner>/<repo>#<skill>` 标注，并加一段 `Originated from`。
5. **改造后三重验证**：① 目录结构完整 ② frontmatter 契约完好 ③ 关键命令 dry-run 走通。
6. **产出**：待装清单 → **用户拍板 → 直接装**（clone → copy/symlink），装完能出现在 `<verify_cmd>` 的输出里即止。

---

## 落地规程（local-adapt）

铁律 7 的执行细则。**所有**外部 skill 走完三道工序后，进入本规程**强制本地化**，绝不原样装：

1. **领名 + 建档**：本地改造版用新 skill 名（如 `vercel-react` → 本地 `react-patterns-local`），
   在 `<report_dir>` 建一条存根：`source: <owner>/<repo>#<skill>` + 改造日期 + 改造类型（①/②/③）+ 改动清单。
2. **决策点清单**：改正文前，先产出改造决策点清单 → **用户逐项拍板** → 被否的行按用户意思改后再继续。
3. **重写 / 融合 SKILL.md**：
   - 读本地约定 → 正文与注释**与用户母语一致**（术语除外）；
   - 加本地约束：文件落工作盘、本地工具范式、交付方式、结论先行 + 证据分层；
   - 命中**铁律 8 隐藏价值观** → 剥壳重定本地标准，不搬运外部价值观；
   - 加 `source:` 标注 + `adaptVersion:` 起步（**这是被改造产物的版本号，与本 skill 的 version 无关**）。
4. **脚本合规化**：审查 scripts（路径合法、不落系统盘、无凭据外传、不装额外依赖）。
5. **三重验证**：`scripts/verify-adaptation.sh` ① 目录完整 ② frontmatter 契约 ③ 关键命令 dry-run。
6. **报告 + 拍板**：给出「本地改造版」与「原版」的 diff 摘要（含决策点清单结论）→ 用户拍板才 install。
7. **装后更新**：能出现在 `<verify_cmd>` 输出里即视为装好；同步更新台账与本地记忆。

> 例外（走 ④只借鉴不装）：判「重复 / 无价值 / 安全否决」时，只往 `<report_dir>` 存一行借鉴笔记，不搬本 skill。

---

## 不适用场景

| 不适用 | 走哪儿 |
|---|---|
| 从 0 创作一个全新 skill | 创作类 skill（skill-creator 之类） |
| 只想搜一下有没有某个 skill | 检索类 skill，不要走三道工序 |
| 修改本地已有的 skill | 那是迭代，不是适配 |
| 定期体检整个技能库 | 那是另一件事；本 skill 明确**不做**订阅 / 定期扫描 |
| 已经确定要装、只想问怎么装 | 直接装，别套流程 |

---

## 文件地图

| 文件 | 用途 |
|---|---|
| `SKILL.md` | 本文件（配置 + 铁律 + 三道工序） |
| `references/source-routing.md` | 各类来源 → 对应解析器映射 |
| `references/baseline-scan.md` | 三层基线扫描规则 |
| `references/profile-reading.md` | 读本地约定指引（不建快照） |
| `references/decision-points.md` | 改造决策点清单模板 |
| `scripts/gitclone.sh` | 暂存区 clone（下载不安装） |
| `scripts/make-adaptation.sh` | 按类型改写 SKILL.md（组装 / 改造 / 定制） |
| `scripts/verify-adaptation.sh` | 三重验证 |
| `skill-reports/` | **真实审查报告样例** —— 想知道产出长什么样，先看这里 |
| `CHANGELOG.md` | 本 skill 自身版本更新记录 |

---

## 关于本文件

- **出处**：思路与流程受 `anthropics/skills` 生态启发，独立适配改造完成。
- **许可**：MIT，详见仓库根目录 `LICENSE`。
- **它是什么**：一套**装前审查方法**。方法部分与机器无关，可以照搬；
  配置块那 8 行是**给你改成自己的**。
