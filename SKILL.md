---
name: skill-adapt
description: "把「要装的新 skill」当原料，经三道工序（价值分析→整合分析→适配改造）适配到本地后给用户拍板，装不装由用户定。当用户拿来一个新 skill（B站链接/公众号文/GitHub 仓库/或说『帮我装 xxx skill』），或想评估某个外部 skill 是否值得装、怎么改造得贴合本地时使用。"
allowed-tools: [Bash, Read, Write, Glob, Grep, WebFetch, Skill]
version: 1.2.0
---

# 技能适配（skill-adapt）

把外部 skill 当作「原料」，经三道工序（**价值分析 → 整合分析 → 适配改造**）处理后，产出「已适配到本地、可拍板可装」的成品。**本 skill 只做『怎么装』的决策辅助，不替用户拍板；装不装永远由用户定。**

## 定位：skill 分两类

```
自建 skill（从 0 创作） ── 走 skill-creator（先 grill 磨需求 → creator 打磨评测）
外部 skill（要装进来）  ── 走 skill-adapt（三道工序 → 本地化改造 → 用户拍板）
                              └─ 铁律：外部 skill 一律走适配改造，绝不直接装
```

## 铁律（最高优先级）

1. **永远被动触发**：不主动跑。用户给了新 skill 来源才开工；装完确认后即止，不做订阅/定期扫描。
2. **下载不安装**：外部 skill 一律 `git clone --depth 1` 暂存区，**绝不**直接 `npx skills add` 装进来。
3. **三层基线扫描**：判断「本地已有的」从 3 层看——agent 技能层 / 生态登记层 / 办公套件内置能力层。外部协作类 skill 与内置层撞 = 能力重复（有更好替代），**不**等同「已安装」。
4. **安全栏一票否决**：价值分析第 5 项固定栏。发现 SKILL.md / scripts 有可疑外传（curl 传数据到未知域名）、请求敏感凭据、操作禁路径 → **标红不装**，并给理由。
5. **D 盘原则**：暂存区、报告库、改造产物一律落 D 盘，禁止 C 盘内容。
6. **只决定怎么装，不产生新安装**：三道工序各自结束处有决策门，等你拍板；你把关→我直接装（一条 install 链路跑完），判定标准 = `npx skills list`（PowerShell）出现该技能，即视为装好，即止。
7. **默认强制适配改造，绝不裸装**：**所有**外部 skill 一律先经三道工序改造（①组装/②改造/③定制），产出**本地改造版**后才允许进入体系；例外仅两档——判「重复/无价值」或「安全一票否决」时走 ④只借鉴不装（存借鉴笔记、不搬正文）。**没有「直接原样装进来」这条路径**。
8. **隐藏价值观一票识别（剥壳不否决）**：价值分析第 5 项固定栏。外部 skill 里凡出现 **评分/分数、分级/成长等级、会话数据回流算分、趋势/weakness 追踪、督促/复盘督促** 等字眼 → 判定作者把自己的**价值观考核体系**包装进了 skill。处置：**不直接搬运**，剥壳重定本地标准（「评价人」→「评价指令」）；涉及删除/替换决策进改造决策点清单由用户拍板；「带考核性质」不等于「安全否决」，不因价值观异同拒绝安装。

## 三道工序（对应原始三步）

### 第 1 步 · 价值分析（检 + 过滤）

**触发**：收到新 skill 来源（链接/文章/指令）。
1. **解析来源**：按 [source-routing.md](references/source-routing.md) 的映射把输入转成 skill 名单——
   - B站链接 → 复用 bili-video-digest skill 拆内容；
   - 公众号/文章/网页 → WebFetch 抓正文；
   - GitHub 仓库 → curl 拉 README + 找 SKILL.md；
   - 纯文字/「帮我装 xxx」→ 当关键词，走 `npx skills find` + WebSearch 补齐。
2. **下载不安装**：`git clone --depth 1` 到 `D:\ClaudeCode\skills-staging\<owner__repo>\`，只读 SKILL.md + references。
3. **三层基线扫描排除**（见铁律 3）：
   - 已装在 `~\.claude\skills\`（agent 层）+ `npx skills list -g`（生态登记层）→ 排除；
   - 办公套件内置能力（能力层）→ 当「能力替代品」比较。
4. **全网检索**：WebSearch + skills.sh 生态，罗列有无其它类似 skill，判断互补/重复/独立。
5. **安全栏**（见铁律 4）+ **隐藏价值观检查**（本轮新增，铁律 8）：技能里凡出现「评分/分数」「分级/成长等级」「会话数据回流算分」「趋势/weakness 追踪」「督促/复盘督促」等字眼 → **判定其作者把自己的价值观考核体系包装进了 skill**。这类内容对用户是**异物**：要么剥壳重定标准（把「评价人」改成「评价指令」），要么不进正文，必要时走决策点清单让用户拍板。示例：prompt-boost 原版 15 维评分 = 安克价值观量表包装（第一性/求极致/共成长），适配时被识别剥除。
6. **输出**：**四段式分析**（本轮新增，替代此前单薄矩阵）——
   ```
   候选 skill × 三层基线（重复/互补/无交叉 + 理由）
   ① 工作原理：它怎么转起来（触发机制 / 各步骤动作 / 依赖什么输入输出）
   ② 设计意图：作者想解决什么问题（站在作者视角推断，不臆断）
   ③ 适用边界：什么场景有效、什么场景失效
   ④ 隐含假设与本地张力：它对环境的预设（如有无考核/评分/回流/督促结构）→ 与本地原则（画像/铁律/两阶段协议）有无冲突点
   结论：值得装 / 不装（重复/无价值/安全否决）→ 理由
   ```
   价值分析**一次输出，不做多轮追问**；有拿不准的判断标注（事实/推断/待确认），写进待确认清单让用户定夺。
7. **等你决策** → 有价值 → 进第 2 步；无价值 → 借鉴笔记留档（见 Q26），流程结束。

### 第 2 步 · 整合分析（比 + 合）

对候选 skill vs 本地相似 skill：
1. 出**优劣对比表**。
2. **近亲逐个四行对话**（本轮新增，扩展对比）——不只看撞名，凡「领域相近/职责相近」的都逐个过：
   ```
   候选 vs 近亲（grilling / creator / office-hours / prompt-boost / 本地既有 skill…）
   a. 关系：互补 / 重复 / 张力（为什么）
   b. 取舍建议：哪个留、哪个并、怎么分工
   c. 若合并：合并后职责边界一句说清
   ```
3. 拆散 → 重组 → 合并且标注：**本地的都用本地、外部更强的用外部、重复的剔除**。
4. 输出**整合方案** → **等你确认**。

### 第 3 步 · 适配改造（改 + 造）

**流程反转（本轮核心改动）：先出决策点清单逐项拍板，再写正文。改动决策前置，杜绝自作主张。**

1. **读画像**：`profile-reading.md` 指引读 MEMORY.md 索引 + 所有相关记忆（不只全局）。
2. **定改造类型**（四档，Q24 定 ①为默认，每次由用户拍板）：
   - **① 组装**（默认）：外部系列 skill 拆散重组成本地一套模块；
   - **② 改造**：拿到外部 SKILL.md 改写其运行逻辑（加目录规范、改命令、强约束）；
   - **③ 定制**：外部只当参考模板，本地主力重建；
   - **④ 只借鉴不装**：无价值/重复/有风险 → 存一条「借鉴笔记」（一行要点+来源链接）到 `skill-reports/`，不搬正文。
3. **产出「改造决策点清单」**（本轮新增，置于任何改动之前）——把拟改动逐项列出，让用户逐项拍板：
   ```
   决策点清单（改什么 / 为什么 / 不改会怎样 / 恢复代价）
   │ 决策点 | 现状→建议 | 原理/理由 | 不改会怎样 | 恢复原版代价 | 拍板
   │────────┼──────────┼──────────┼────────────┼──────────────┼────
   │ 1.     │          │          │            │              │ (等拍)
   │ 2. … 用决策点模板
   ```
   每一项都要写**原理和理由**，不是只写「砍掉 XX」；所有改动先经用户拍板，才能进下一步写正文。**典型反面教训（prompt-boost v1.1.0）**：当时自作主张砍掉被动识别模式、只给了结论没给理由，用户追问才发现误判——决策点清单就是要杜绝这类返工。
4. **写正文**（拍板通过后才动 SKILL.md）：**改造落点**：**D 盘**（`D:\ClaudeCode\skills\`），保留 `source: <owner>/<repo>#<skill>` 标注 + SKILL.md 加 `Originated from` 段，frontmatter 加 `version: 1.1.0`。
5. **隐藏价值观剥壳**（命中铁律 8 时）：把外部评分/分级/回流话术替换为本地标准（如「评价人」→「评价指令」），不直接搬运外部价值观。
6. **改造后三重验证**：①目录结构完整 ②frontmatter 契约完好 ③关键命令 dry-run 走通。
7. **产出**：待装清单 → **你拍板 → 我直接装**（`git clone`→copy/symlink 链路），装完确认出现在 `npx skills list` 即止。

## 使用方式

用户说「**帮我适配 [B站链接/公众号文/GitHub 仓库/我要装的 xxx skill]**」→ 本 skill 触发三道工序 → 每道工序结束处等你决策 → 最后你拍板 → 直接装。

## 文件地图

| 文件 | 用途 |
|---|---|
| SKILL.md | 本文件（导航 + 铁律 + 三道工序） |
| references/source-routing.md | 三类来源 → 对应解析器映射 |
| references/baseline-scan.md | 三层基线扫描规则 |
| references/profile-reading.md | 读画像指引（不建快照） |
| references/decision-points.md | 改造决策点清单模板（见文末「六步落地规程」第 5 步） |
| scripts/gitclone.sh | 暂存区 clone（下载不安装） |
| scripts/make-adaptation.sh | 按类型改写 SKILL.md（组装/改造/定制） |
| scripts/verify-adaptation.sh | 三重验证 |
| local-adapt.md（见下） | 「外部一律改造后装」的**本地落地规程** |
| CHANGELOG.md | 本 skill 自身版本更新记录 |

## local-adapt：外部 skill 的本地落地规程（铁律 7 的执行细则）

**所有**外部 skill 走完三道工序后，进入本规程**强制本地化**，绝不原样装：

1. **领名 + 建档**：本地改造版用新 skill 名（如 `vercel-react` → 本地 `react-patterns-local`），在 `skill-reports/` 建一单存根：`source: <owner>/<repo>#<skill>` + 改造日期 + 改造类型（①/②/③）+ 改动清单。
2. **[决策点清单]**：改正文前，先产出 `references/decision-points.md` 模板的**改造决策点清单** → **用户逐项拍板** → 有被否的行按用户意思改后再继续。（这是适配改造第 5 步，放到落地规程里强化「先拍板再写」）。
3. **重写/融合 SKILL.md**：
   - 读[画像](references/profile-reading.md) → 正文/注释**全中文**（术语除外）；
   - 加本地约束：文件落工作盘、字段 ID/口径/数据连接等**本地工具范式**、交付用 file://、结论先行+证据分层；
   - 命中**铁律 8 隐藏价值观** → 剥壳重定本地标准（评分→自检、回流→模板库），不搬运外部价值观；
   - 加 `source:` 标注 + `version: 1.1.0` 起步 + 文末「关于本文件」指向 skill-adapt 台账。
4. **脚本合规化**：本地化可证 scripts（路径用 `D:\`、无 C 盘、无凭据外传、不装额外依赖）。
5. **三重验证**：脚本 `verify-adaptation.sh` ①目录完整 ②frontmatter 契约 ③关键命令 dry-run。
6. **报告 + 拍板**：给出「本地改造版」与「原版」diff 摘要（含决策点清单结论）→ 你拍板才 install。
7. **装后更新**：确认出现在 `npx skills list`（PowerShell）即视为装好；同步更新本台账 + 记忆 `reference_claude-skills.md`。

> 例外（走 ④只借鉴不装）：判「重复/无价值/安全否决」时，只存一行借鉴笔记进 `skill-reports/`，不搬本 skill。

## 与 find-skills 的关系

find-skills 是「底层检索器」（裸检索、找到就装）；本 skill 在它之上做「分析 + 过滤 + 适配」。检索动作一律走 find-skills（读它）或 `npx powershell -Command "npx skills find|add|list"`，不重复造检索器；本 skill 只管编排。